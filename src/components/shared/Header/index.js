import React from 'react'
import NavBar from "@components/shared/NavBar";
import './style.scss'

const Header = () => {
  return (
    <header id="header">
      <NavBar />
    </header>
  );
};

export default Header;
